{
  "name": "shop-spa-qa",
  "version": "1.0.0",
  "description": "QA automation for Shop SPA",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "wait-on": "^8.0.1"
  }
}